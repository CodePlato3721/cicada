# cicada 项目 Context

## 项目定位

Discord 实时语音翻译 Bot 的 POC(概念验证)阶段。目标是验证"语音进 → 翻译 → 语音出"这条完整链路在 Discord 语音频道场景下的可行性、延迟表现和翻译质量,暂不考虑商业化、公开发布、多租户等问题。

**当前阶段目标仅限于**:
- 跑通最小可用链路（已完成：/join 后自动监听频道语音，本地 Silero VAD 实时切句 → Groq Whisper STT → Groq Llama 翻译 → Groq TTS(Orpheus) 播回频道）
- 实时翻译：一句话被 VAD 切出来就立即进入翻译流程，同时继续采集下一句，不等整段结束再处理（采集与翻译并行；语音播放本身仍需排队串行，避免多句声音在频道里叠在一起）
- 触发方式改为常驻监听：`/join` 即自动开始监听频道内所有人的语音，不需要额外命令触发；`/leave` 停止监听并退出

**明确不做的事**(POC 阶段范围外):
- 不接商业化/订阅/付费逻辑
- 不做多租户(guild_id → API key 映射)
- 不申请 Discord App 公开发布/审核
- 不做手机 companion app(先验证 Discord bot 本体)

---

## 核心技术决策

### 服务端语言:Node.js

选择理由:Discord 语音接收(接收多用户音频流、按 SSRC 区分说话人)这个环节,`@discordjs/voice` 是官方维护、文档完整、有生产环境验证的方案。对比 discord.py,语音接收功能长期依赖社区第三方扩展,成熟度不足。这是决定语言选择的核心因素,其余业务逻辑（调用外部 API、异步等待）两个语言都能胜任,不构成决定性差异。

### 核心依赖库
- `discord.js` + `@discordjs/voice`:Discord 连接与语音收发
- Silero VAD:语音活动检测,**本地运行**,不联网,判断"一句话是否说完"

---

## 架构流程

```
Discord 语音频道
      │
      ▼
Node.js 服务器(本地开发机 / 后续部署到 DigitalOcean)
      │
      ├─ discord.js 接收每个用户的 Opus 音频流(按 SSRC 区分说话人)
      ├─ 解码为 PCM
      ├─ Silero VAD 本地切句(检测静音间隔,确认一句话边界)
      │
      ▼
Groq Whisper Large v3 Turbo(STT,联网调用)
      │  → (speaker_id, 原文, 源语言)
      ▼
Groq Llama 3.1 8B Instant(翻译,联网调用)
      │  → 译文
      ▼
Groq TTS - Orpheus(语音合成,联网调用,流式)
      │
      ▼
返回 Node.js 服务器 → 播放回 Discord 语音频道 / 或输出文字字幕
```

**关键决策:STT + 翻译 + TTS 三个环节全部收敛在 Groq 一家**,不再引入 DeepSeek 或其他供应商。理由:简化架构,减少跨服务商网络调用带来的额外延迟环节;且 Llama 3.1 8B Instant 价格($0.05/$0.08 每百万 token)比 DeepSeek 官方 API 更低,而 Groq 的 TTFT 在多份第三方评测中排名第一梯队,延迟优势明显。

---

## 具体模型与定价参考(2026年8月数据,建议实际调用前在 Groq 控制台核实最新价格)

| 环节 | 模型 | 价格 | 备注 |
|---|---|---|---|
| STT | whisper-large-v3-turbo | $0.04/小时音频 | 每次请求最低按10秒计费,VAD切句粒度会影响实际成本 |
| 翻译 | llama-3.1-8b-instant | $0.05/M输入,$0.08/M输出 | 通用小模型,翻译质量需实测验证是否够用 |
| TTS | Orpheus (canopylabs/orpheus-v1-english) | $22/M字符(英语) | **仅支持英语和阿拉伯语,不支持中文** |

### 已知限制:TTS 不支持中文
Groq TTS(Orpheus)目前只有英语和阿拉伯语声音。如果 POC 需要验证中文语音播报效果,需要额外引入本地方案(候选:Kokoro-82M,Apache 2.0 可商用,CPU/GPU 均可跑,中文支持较好),这块暂未接入,是后续需要决策的点。

---

## Groq 免费额度(POC 阶段够用,注意分维度限制)

- STT:每天 2000 次请求,每天 28800 秒音频(约8小时)
- 翻译 LLM:约 30 RPM(每分钟请求数)、6000 TPM(每分钟token数)、每天 1000-14400 次请求(不同来源数字有出入,以控制台实际显示为准)
- TTS:免费额度具体数字未查实,需登录 console.groq.com 核实

**注意**:免费额度按维度限制,不是单一总量。VAD 切句过密可能优先触发 RPM/TPM 限制,而不是总请求数限制。单人测试通常够用,多人并发测试容易触顶。

超出免费额度可绑定信用卡升级 Developer tier,不产生额外费用直到实际用量超出,且解锁更高限额 + 约25%折扣。

---

## VAD 切句注意事项

- VAD 本身运算量极小(毫秒级,CPU 可跑),延迟主要来自"等待静音间隔"这个机制本身,不是算法慢
- 静音判定阈值需要作为可调参数设计,不要硬编码:
  - 阈值过短 → 容易把自然停顿误判为句子结束,翻译支离破碎
  - 阈值过长 → 延迟增加
- 切句粒度直接影响 Groq STT 成本(10秒最低计费门槛),需要平衡"响应速度"与"成本效率"

---

## 场景相关的产品设计考量(暂不实现,记录供后续参考)

不同使用场景对延迟/缓存/上下文的需求差异很大,可能需要分模式处理:

| | 战斗语音喊话 | 战术讨论会议 |
|---|---|---|
| 句子特点 | 短、固定、高频重复 | 长、即兴、具体细节多 |
| 缓存策略 | 精确匹配+语义缓存收益高 | 基本不适合缓存,可能引入错误 |
| 上下文依赖 | 低 | 高,需要带最近几句对话 |

POC 阶段不需要实现缓存层,先跑通基础链路。

---

## Discord Bot 配置信息

- Bot 名称:`cicada`
- 状态:私有开发用途,未公开发布,不需要经过 Discord 审核流程
- 测试服务器:开发者自建的私人测试 Discord 服务器
- 权限需求(OAuth2 URL Generator 中勾选):
  - `bot` scope
  - 权限:查看频道、发送消息、连接语音、讲话、使用语音活动
- 如涉及消息内容解析,需在 Developer Portal 手动开启 "Message Content Intent"

---

## 部署路径规划(POC 阶段用不到,记录供后续参考)

- 当前阶段:本地开发机运行,不需要 ngrok(Discord bot 走 Gateway WebSocket 主动连出,不是被动接收 HTTP 请求,不需要暴露公网端口)
- 后续正式部署:DigitalOcean Droplet,美国西海岸节点(开发者位于加州,兼顾自身访问与用户延迟)
- 若未来扩展亚洲用户,可考虑 Vultr 补充亚洲节点(首尔/东京/新加坡),Vultr 在亚洲覆盖明显优于 DigitalOcean

---

## 目标用户与语言范围

面向全球用户(除中国大陆地区,因 Discord 在该地区不可用)。产品最终形态希望支持"每个用户各自选择目标语言,收听对应翻译"这种个性化体验,但这在 Discord 原生语音频道架构下无法直接实现(bot 只能向整个频道播放一路音频),需要 companion app 才能做到——这属于后续阶段,POC 不涉及。