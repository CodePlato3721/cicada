# onnxruntime-node 装的时候默认会额外下载 CUDA(GPU)版本的运行时库（几百 MB），
# 这个项目的 VAD 从头到尾只在 CPU 上跑，用不到，白白占安装时的内存/磁盘/带宽——
# 小内存服务器（比如 1GB 的 Droplet）解压这个大文件时甚至会被 OOM killer 杀掉。
# 这行让 npm install 跳过这部分下载，只装 CPU 需要的部分（已经内置在包里，不受影响）。
onnxruntime-node-install=skip
